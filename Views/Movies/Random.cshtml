@model Vlidly.ViewModels.RandomMovieViewModel

@{
    ViewBag.Title = "View";
}

<h2>@Model.Movie.Name</h2>

<ul>
    @foreach(var customer in Model.Customers)
    {
        <li>@customer.Name</li>
    }
</ul>

